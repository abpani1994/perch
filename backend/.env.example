# Database
DATABASE_URL=postgresql://perch:perch@db:5432/perch?schema=public

# Auth — generate with: openssl rand -base64 48
JWT_SECRET=

# Server
NODE_ENV=development
PORT=4000

# Stripe (Pro subscription) — leave empty to run in "not connected" mode
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
CLIENT_URL=http://localhost:4000